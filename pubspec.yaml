name: bravo_connet_v2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 FIREBASE
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0

  # 📸 IMÁGENES
  image_picker: ^1.0.7

  # 📁 ARCHIVOS
  file_picker: ^11.0.2

  # 🔗 ABRIR LINKS
  url_launcher: ^6.2.5

  # 🎥 VIDEO (🔥 AQUÍ ES DONDE VA)
  video_player: ^2.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
