name: bravo_connet_v2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.3.0   # 🔥 CORREGIDO (evita incompatibilidades)

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 FIREBASE (DEBEN IR AQUÍ, NO EN dev_dependencies)
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0

  # 📸 IMÁGENES
  image_picker: ^1.0.7

  # 📁 ARCHIVOS (VERSIÓN COMPATIBLE)
  file_picker: ^11.0.2   # 🔥 IMPORTANTE: NO usar 5.5.0

  # 🔗 ABRIR ARCHIVOS
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
